# 上下文工程基线

## 原则

全量应用知识不得直接放入单次 Prompt。Agent 只接收完成当前任务所需的页面、
相邻路径、相关证据和压缩历史。

## 目标能力

- Working、Task、Long-term 三层上下文
- Agent 独立 Token 预算
- 历史探索压缩
- 语义检索和缓存
- 调用成本、耗时与裁剪记录

## 验证要求

未来每次 LLM 调用都必须可解释其上下文来源，并对超预算行为进行可重复裁剪。
当前工程初始化不实现 Context Manager。
