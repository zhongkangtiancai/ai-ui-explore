# AI UI Explorer 编码规则

## 项目定位

AI UI Explorer 是企业 Web 应用智能探索与知识建模平台，不是网页爬虫，也不是
测试执行平台。核心资产是可追溯的 Application Knowledge Model。

## 架构约束

- 浏览器自动化统一使用 Playwright。
- LLM 调用必须经过 Provider 抽象，不得在业务代码中绑定具体厂商 SDK。
- Agent 调用必须经过 Context Manager，不得直接拼接全量知识库。
- 业务配置通过环境变量或配置文件注入，禁止硬编码 URL、模型名和密钥。
- 模块边界应小而清晰；公开接口必须有测试和文档。

## 证据与安全

- AI 结论必须同时保存结论、证据和置信度。
- 未经受控账号与测试数据验证的数据权限差异，只能标记为推测或待确认。
- DOM、网络响应、日志和截图持久化前必须支持敏感字段脱敏。
- 登录状态、抓包、截图和探索输出默认不进入版本控制。
- 危险操作采用默认拒绝策略。

## 交付约束

- 开发环境允许使用公网模型进行验证；企业环境只依赖 OpenAI-compatible 私有接口。
- 最终离线包必须包含锁定依赖、Playwright 浏览器、容器镜像、迁移脚本和部署手册。
- 未经实际验证，不得宣称外网、内网或离线部署可用。
